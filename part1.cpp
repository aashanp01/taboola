#include <vector>
#include <deque>
#include <stack>
#include <queue>
#include <string>
#include <sstream>
#include <iostream>
#include <cassert>
#include <fstream>
#include <cstdlib>
#include <unordered_map>
#include <algorithm>

//Part 1
class Obj
{
    //incomplete
};
void process(string json)
{
    unordered_map<strin,obj> mp;
    istringstream str(json);
    string predec, word, proced;
    str>>word; // get initial bracket
    unordered_map<string,string> mp;
    while(str>>word)
    {
        if(word == ":")
        {
            str>>proced;
            if(proced == "{")
            {
                
            }
        }
        predec = word;
    }
}
